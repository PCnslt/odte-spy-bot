# Core — backtest + training on real Polygon data. (Live loop also needs requirements-extras.)
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31         # Polygon.io REST client
scipy>=1.10
scikit-learn>=1.3
lightgbm>=4.0
pyarrow>=14.0          # parquet cache for real bars
